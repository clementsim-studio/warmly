// Archived features (DECISIONS D-025, D-029): the code stays in place and
// working, but has no reachable entry point while the flag is off. Flip to
// true to bring a feature back without having to rebuild it.
export const FEATURE_MONETIZATION = false;
export const FEATURE_PREVIEW_AND_SEND_PAGE = false;
